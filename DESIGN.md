# Design tokens & rules

이 문서는 `app.py`의 CSS가 지켜야 하는 값의 전체 목록입니다. **여기 없는 색·크기·반경은 쓰지 않습니다.** 필요하면 이 표를 먼저 고치고, 그 자리에서 새로 만들지 않습니다.

만들어진 이유: 색 94개(회색만 60개), 폰트 크기 33개(1px 이내로 붙은 값이 29쌍)까지 늘어나 있었습니다. 매번 새 컨트롤을 만들 때마다 값을 새로 골랐기 때문이고, 그 결과가 "AI가 만든 것 같은" 인상의 실체였습니다. 선택지를 유한하게 고정하면 그 판단 자체가 사라집니다.

**검사**: `python scripts/check_design.py`로 미등록 색상·대비 미달·굵기·반경 위반을 확인합니다. 문서만으로는 규칙이 안 지켜집니다 — 새 CSS를 추가한 뒤엔 항상 돌려볼 것.

## 강조 예산 (Emphasis budget)

강조는 총량이 정해진 자원입니다. 아래 등급 밖에서 브랜드 그린을 쓰지 않습니다.

| 등급 | 용도 | 표현 수단 | 브랜드 그린(`#76b900`) |
|---|---|---|---|
| 1차 | 지금 어디 있나 (탭) | 2px 밑줄 + 흰 글자 | 씀 |
| 데이터 | 상승 | 초록 숫자 | 씀 |
| 2차 | 무엇을 볼까 (필터·토글) | 표면 밝기 차이만 | **금지** |
| 3차 | 설명, 캡션, 라벨 | 회색 소문자 | **금지** |

**범위 한정 예외 — 드릴다운 패널(`.pt-detail-body`)**: 패널이 열렸을 때는 화면의 초점 자체라 탭바와 강조를 다투지 않습니다. 이 패널 내부의 섹션 라벨(`.ptd-block .ptd-label`)에 한해 accent를 허용하되, **조건은 패널이 다른 곳(티커, `.ptd-ticker`)의 accent를 포기하는 것** — 한 패널 안에서 초록이 두 역할(티커/구조)로 갈리면 뜻을 잃기 때문입니다(2026-08-19). 이 조건 없이 다른 컴포넌트로 확대 금지.

## 중성색 (Neutrals) — 9단계

| 토큰 | 값 | 대비(대`#080808`) | 용도 |
|---|---|---|---|
| `bg` | `#080808` | — | 페이지 배경 |
| `surface-1` | `#0e0e0e` | — | 카드, 배너 |
| `surface-2` | `#14161a` | — | 트랙, 입력 |
| `surface-3` | `#20242b` | — | 선택된 세그먼트 |
| `border-subtle` | `#1a1a1a` | — | 카드 테두리 |
| `border` | `#2a2d33` | — | 컨트롤 테두리 |
| `text-meta` | `#828a94` | 5.73:1 | 캡션, 눈썹 라벨 |
| `text-muted` | `#9aa3b0` | 7.87:1 | 보조 텍스트, 본문 단락 |
| `text-secondary` | `#c3c9d1` | 12.0:1 | 소제목, 강조 캡션 (2026-08-18 추가) |
| `text` | `#f0f1ef` | 15.6:1 | 본문 강조, 활성 상태 |

모든 텍스트 색은 **배경 대비 4.5:1 이상**(WCAG AA)이어야 합니다. 새 회색을 추가하기 전에 실제 배경 위에서 대비를 계산할 것.

**예외 2가지**: ①밝은 배경 위의 어두운 글자(사이드바 선택된 언어 토글 — 주황 그라디언트 필 위 `bg` 토큰값 글자). 대비 계산은 실제로 맞닿는 배경 기준이라 페이지 배경(`#080808`)과 비교하면 오탐이 남. ②`border` 토큰(`#2a2d33`, 원래 `#2a2a2a`) — 데이터 없음(N/A) 표시용 흐린 대시(`–`/`—`) 2곳 한정(`app.py` 2564, 2574줄). "여기 볼 게 없다"를 시각적으로 전달하는 의도된 저대비. **주의**: 같은 색이 한때 푸터(출처·시각 텍스트)에도 쓰여 있었는데 그건 실제 콘텐츠라 예외가 아니라 버그였음(1.4:1) — 발견해서 `text-meta`로 수정(2026-08-18). 새로 이 색을 텍스트에 쓸 땐 정말 N/A 자리인지부터 확인할 것.

**`text-secondary` 추가 경위**: 회색 치환 작업 중 명도 192~212 구간에 `#c0c0c0 #c4ccd6 #c9ced6 #d0d0d0 #cbd5e1 #d1d5db` 6종이 몰려 있는 걸 발견. `text-muted`(162)와 `text`(240) 사이가 비어 있어 이 6종을 억지로 양끝에 붙이면 40 이상 점프가 생김 — v2 리뷰 때 겪은 "표가 실제 쓰임새를 못 감당하는" 패턴 재발이라 표 자체를 보강.

## 의미색 (Semantic) — 2계층

**상태**

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#76b900` | 현재 위치, 상승 강조 (강조 예산 1차·데이터에서만) |
| `alert` | `#c87f00` | 신규, 주의 |
| `up` | `#5a9e3a` | 상승 수치 |
| `down` | `#e05656` | 하락 수치 (이전 `#a03030`은 대비 2.8:1로 AA 미달이라 교체, 2026-08-18) |

**분류**

| 토큰 | 값 | 용도 |
|---|---|---|
| `badge-core` | `#4a90d9` | CORE 배지, 13F 증가 |
| `badge-partner` | `#9370d8` | PARTNER 배지 (이전 `#7c5cbf`는 3.95:1로 AA 미달, 2026-08-18 교체) |
| `filing-new` | `#22c55e` | 13F 신규 |
| `filing-increase` | `#4a90d9` | 13F 증가 (`badge-core`와 동일) |
| `filing-decrease` | `#e05656` | 13F 감소 (`down`과 동일) |
| `filing-exit` | `#8b949e` | 13F 청산 |
| `filing-hold` | `#6366f1` | 13F 유지 |

**예외 — 데이터 시각화 팔레트**: `CAT_COLORS`(섹터 분류 9색), `plotly` `Light24`(종목별 라인 구분)는 이 표 밖입니다. 조각·라인을 구분하는 게 목적이라 색 수를 줄이면 기능이 깨집니다. 단, **이 팔레트를 버튼·배지·본문 텍스트 등 UI 크롬에 쓰는 것은 금지**합니다.

**예외 — 배지 틴트 짝(badge tint pair)**: `filing-*`/13F 필터 배지는 `(반투명 배경, 밝은 글자)` 쌍으로 이루어져 있습니다 — 배경은 위 표의 색을 18% 알파로, 글자는 그 색의 밝은 변형(`#4ade80`·`#7ab8f5`·`#f08a8a`·`#a5a8f5`·`#b0b8c2`)을 씁니다. 반투명 배경 위에서 표 색을 그대로 쓰면 안 읽혀서 필요한 구조입니다. **미등록 색상처럼 보여도 이 5색은 중복이 아니라 짝꿍**입니다 — `check_design.py`가 아직 이 패턴을 모르고 전부 미등록으로 잡습니다(개선 여지).

**예외 — 배지 보더 틴트**: `.badge-core/new/seed/partner`와 알림 배너는 각자 텍스트색의 어두운 변형을 테두리로 씁니다(`#1e3a5f`·`#3d2600`·`#2a3f00`·`#2a1a4a`·`#2a2200`, 순서대로 파랑/주황/초록/보라/주황 배지·배너 짝). 2026-08-18 첫 검사에서 "정리 대상"으로 잘못 분류했다가 실제로 각 배지 고유색과 짝지어진 의도된 패턴인 걸 확인해 정정 — `border`(중립 회색) 토큰으로 합치면 배지별 색 구분이 오히려 사라짐. 배지 틴트 짝과 같은 이유의 별개 예외로 유지.

**예외 — 외부 플랫폼 브랜드색**: 공유 버튼 호버는 해당 플랫폼의 실제 브랜드색을 씁니다(`#2aabee` 텔레그램). 우리 팔레트가 아니라 상대 브랜드를 따라야 하는 자리라 토큰화 대상 아님.

**예외 — 별점(rating) 금색**: 피드백 폼의 `⭐` 표시 색(`#fbbf24`)은 `alert`(`#c87f00`)보다 밝고 채도 높은 게 의도적입니다(평점 표시 관용 색). 별도 의미(신규·주의)를 가진 `alert`와 섞이면 안 돼 분리 유지.

## 타이포 — 7단계

| 토큰 | px | 용도 |
|---|---|---|
| `eyebrow` | 10 | 대문자 라벨. **대문자는 여기서만 허용** |
| `caption` | 11 | 캡션, 각주 |
| `small` | 12 | 보조 수치, 배지 |
| `body` | 13 | 기본 UI, 탭, 컨트롤 |
| `subhead` | 15 | 섹션 제목 |
| `title` | 20 | 화면 제목 |
| `metric` | 26 | 지표 큰 숫자 |

**예외 — 사이드바**: Streamlit 네이티브 타이포(Source Sans, 14px대)를 그대로 따릅니다. 위젯 내부 구조를 상대로 강제로 스케일을 맞추면 다른 곳(라벨 `<p>` 등)에 새는 CSS 충돌이 생깁니다 (2026-08-18에 전역 `p{color:#606060}` 규칙이 탭·토글 라벨까지 덮어쓴 사고 참고).

**파일럿 적용 — DETAILS 패널(2026-08-19)**: `.pt-detail-body` 내부(`.ptd-*`)가 이 7단계를 실제로 적용한 첫 컴포넌트입니다. 크기 7종(9.28~16.8px, 1.6px 안에 4종이 몰려 사실상 3단계로 붕괴돼 있었음)이 5단계(10/11/13/15/20px)로 수렴했습니다. 나머지 코드 전체(폰트 선언 46종)로 넓히는 건 별도 라운드(7-1) — 여기서 확정한 매핑을 그대로 따릅니다.

## 자간 — 3단계 (2026-08-18: 18종 → 3값 수렴 완료)

| 토큰 | px | 용도 |
|---|---|---|
| `0` | — | 본문, 버튼, 헤딩(h2/h3) 기본값 |
| `0.3px` | — | 티커 심볼, 소형(≤11px) 보조 텍스트(LIVE/CLOSED 배지 등) |
| `0.8px` | — | `eyebrow` 대문자 라벨 전용(배지, 지표 라벨, 알림 제목 등) |

**예외 2개** (허용 목록에만 존재, 스케일엔 미포함):
- `-0.5px` — 대형 숫자·h1 광학 보정. 큰 사이즈에서 시각적으로 벌어져 보이는 걸 좁혀 잡는 용도.
- `1px` — `.nv-title` 브랜드 워드마크(픽셀 폰트 'Press Start 2P'는 자간 없이 붙으면 낱자가 뭉개짐). `.nv-logo`(굵기 900)와 같은 성격의 예외.

`h3`는 섹션 제목(뉴스·13F·타임라인 등)이라 **대문자 변환도 제거**했다 — 대문자는 `eyebrow`에만 허용(금지 목록 3번). 한글 텍스트엔 `text-transform:uppercase`가 무효이지만, 이전 `letter-spacing:1.4px`는 한글에도 적용돼 자간이 불필요하게 벌어져 있었다.

## 반경 — 3단계 (2026-08-18: 2px/4px 총 26곳도 수렴 완료 — 이제 코드 전체가 이 3값뿐)

| 토큰 | px | 용도 |
|---|---|---|
| `radius-sm` | 3 | 배지, 칩 |
| `radius-md` | 6 | 카드, 트랙 |
| `radius-lg` | 10 | 최외곽 컨테이너, 팝업 |

**캡슐(높이의 절반) 금지.** 세그먼트 트랙·필터 등 2차 컨트롤은 `radius-md`를 씁니다.

## 굵기 — 3단계

`400` 본문 / `500` UI 라벨 / `600` 강조. **700 이상 금지.**

## 여백

4의 배수만 사용: `4 8 12 16 24 32`

## 금지 목록

1. **이모지를 아이콘·강조로 쓰기** — 지워도 뜻이 그대로면 장식. 예외: 별점 슬라이더의 `⭐`(지우면 평점 표현 자체가 사라져 기능), 파비콘 `🟢`(단일 식별 마커), 관리자 전용 텔레그램 알림(방문자 비노출)
2. **캡슐 반경 + 채도 100% 배경** — 2차 이하 컨트롤 금지
3. **대문자 + 넓은 자간** — `eyebrow`(10px) 라벨 외 금지
4. **표에 없는 색·크기** — 필요하면 이 표를 고치지, 그 자리에서 새로 만들지 않기

## 알려진 위반 (정리 대기)

`python scripts/check_design.py`로 확인. 2026-08-18 네 라운드에 걸쳐 전부 해소 — **현재 `PASS`** (예외로 남긴 대비 미달 1건은 알고 있는 것, 위 "예외" 조항 참고). 앞으로 체커가 새 위반을 잡으면 여기 추가하고, 오늘처럼 라운드를 나눠 정리할 것.

**체커 대비 검사의 한계 — 알려진 오탐 2건**: 체커는 모든 `color:`를 페이지 배경(`#080808`) 위에 있다고 가정한다. 실제로 밝은 표면 위에 얹히는 글자는 오탐으로 잡힌다.
- `#2a2d33` (1.45:1) — N/A 대시 관용구
- `#080808` (1.0:1) — 피드백 FAB 아이콘. accent(`#76b900`) 배경 위라 **실측 8.31:1**로 AA 통과. (흰 아이콘이면 2.13:1로 오히려 미달이라 어두운 색이 맞다.)

## 완료된 정리 (2026-08-18)

- 사용자 노출 이모지 41곳 중 16곳(⚠️ 지분투자 아님 8곳, 💡 차트 힌트 4곳, 🔍 필터 라벨 2곳, ⚠️ 사이드바 면책 2곳) 제거. 나머지 9곳은 기능적 예외(위 금지 목록 1번 참고)
- **접근성 버그 18곳 수정** — 회색 치환 작업 중 텍스트 대비가 1.37~2.8:1(AA 기준 4.5:1)까지 떨어진 사례를 다수 발견. 뉴스 메타·카드 라벨·지표 라벨·툴팁 구분점 등 14곳(`#3a3a3a #404040 #505050 #707070 #3a4048`)을 `text-meta`(5.73:1)로. 13F 필터 꺼짐 상태는 `opacity:.55`로 배경째 흐리던 방식이 알파 합성 후 실제 대비 1.37:1까지 떨어져 있어, opacity를 걷어내고 배경·글자를 각각 지정하는 방식으로 교체(5.6:1). 배지 3종(`badge-partner` 3.95→5.29:1, `badge-watch`/`badge-exited` 2.69·1.59→5.73:1)도 3자리 hex(`#555 #333`)라 첫 스캔에서 누락됐던 걸 재검으로 발견해 수정. 푸터(출처·시각 텍스트)도 N/A 대시와 같은 색(1.4:1)을 잘못 쓰고 있던 걸 라이브 대비 재스캔으로 발견해 수정. `.negative`(어제 발견분)까지 합치면 오늘 세션에서만 접근성 버그 6개 계열이 나왔음.
- **회색 28종/75회를 10단계 토큰으로 치환** — 명도 차이가 평균 채널 6 이하(육안 구분 불가)인 값만 대상. 나머지(`badge-*` 등 이미 위 항목에서 처리된 것 제외)는 실질 변화가 있는 그룹이라 별도 검토 후 처리.
- **반경 26곳 수렴** — `2px→3px`(12곳), `4px→6px`(14곳). 이제 코드 전체가 `radius-sm/md/lg`(3/6/10px) 세 값뿐.
- **사이드바 언어 토글(KOR/ENG) 세그먼트 트랙 교체** — 캡슐(`radius:20px`) + `weight:800` + 자간 `2px` + 선택 시 오렌지 그라디언트 풀칠, 금지 목록 3개 동시 위반이었음. `sector_basis`/`main_tabs`와 같은 트랙 어휘로 통일(`.st-key-lang_pills` 스코프, 구/신 Streamlit 셀렉터 병기).
- **`scripts/check_design.py` 작성** — 미등록 색상·대비 미달·굵기·반경 위반을 표준 라이브러리만으로 검사. 첫 실행에서 남은 부채(빨강 3종 중복, Group B 회색, 굵기 5곳)를 정확히 잡아냄 — 위 "알려진 위반" 절에 기록.
- **빨강 3종 중복 통합** — `#ef4444`(5곳: YTD 막대차트, 뉴스 탭 등락색, 13F 타임라인)와 `#e05050`(1곳: 툴팁 YTD)을 `down`(`#e05656`)으로. `#f08a8a`는 통합 대상이 아니라 배지 반투명 배경 위에서 읽히는 밝은 짝꿍색(같은 패턴의 `#4ade80`·`#7ab8f5`·`#a5a8f5`·`#b0b8c2`와 동일 역할)이라는 걸 확인해 SEMANTIC 표에 "배지 틴트 짝" 예외로 명문화하고 체커 허용 목록에도 반영(37→32종).
- **Group B 회색 15종/33회 정리** — 실제 밝기 차이가 있어 보류했던 그룹. `text`(밝은 쪽 8종: `e0e0e0 e5e7eb e7e7e7 e8e8e8 f9fafb ffffff` 등), `text-secondary`(중간 5종: `c0c0c0 c4ccd6 d0d0d0 d1d5db cbd5e1`), `text-muted`(`b0b0b0`), `text-meta`(`6b7280 7a8290 8b93a0`) 세 자리로 흡수. `#6b7280`은 예전 세션에서 이미 "다크 배경 저대비"로 확인돼 `#828a94`로 고쳤던 색인데, 오늘 코드엔 3곳이 원복돼 남아있었음 — 같은 원칙 재적용. 미등록 색상 32→17종.
- **font-weight 700/900 마지막 4곳 → 600** — `.ptd-ticker`, `.tag-guide-name`, 13F 툴팁 NEW 태그, 13F 필터 금액. 전부 색으로 이미 강조되고 있어 굵기까지 겹칠 필요 없었음. 헤더 로고(`.nv-logo`, 900)만 브랜드 워드마크로 예외 유지(체커에도 반영).
- **배지·트랙 보더 재분류** — `#1e3a5f #3d2600 #2a3f00 #2a1a4a #2a2200`(배지·배너 보더)와 `#9ee23a`(LIVE 배지 라이트그린)는 "정리 대상"이 아니라 각각 배지 틴트 짝의 연장선(보더 버전, accent의 밝은 짝)이었던 걸 확인해 예외로 재분류. `lang_pills` 트랙의 `#1c1f25`/`#2b3038`는 반대로 진짜 실수 — 같은 트랙인 `sector_basis`가 이미 쓰던 토큰(`#1a1a1a`/`#0e0e0e`/`#2a2d33`)과 값이 갈라져 있던 걸 맞춤.
- **나머지 자잘한 보더 6곳 정리** — `badge-watch` 보더, 스크롤바 thumb, 밸류체인 빈 박스 보더, 청산 그룹 헤더 바를 `border`/`border-subtle` 토큰으로. `#3b82f6`(13F 타임라인 hold색)는 `filing-hold`와 같은 뜻인데 값이 갈라져 있어 `#6366f1`로 통일. `#2aabee`(텔레그램 공유 버튼 호버)·`#fbbf24`(별점 금색)는 외부 브랜드색·관용색이라 예외로 유지.
- **체커가 CSS 주석 속 hex까지 오탐하던 것 수정** — `/* 이전 #a03030은... */` 같은 설명용 언급을 실제 스타일로 착각해 "미등록 색상"에 잡던 문제. `/* */` 블록을 제거하고 스캔하도록 변경.
- **`python scripts/check_design.py` → PASS 도달** (대비 예외 1건 제외). 미등록 색상 94→0, font-weight 700+ 위반 0(브랜드 예외 1개만 남음), 반경 8종→3종.
- **DETAILS 패널(`.pt-detail-body`) 개편** — 모바일 인라인 경로만 `max-width:100%`로 폭 캡이 빠져 있어 340px 전용 타이포(9.28px 라벨 등)가 660px 폭에 그대로 렌더되고 있었음(한 줄 65자, 한글 편안한 범위 35~45자 초과). `max-width:520px`(13px 한글 40자 기준)로 복원. 투자액을 `.ptd-hero` 카드로 분리하고 3섹션(요약/투자근거/구조)을 `.ptd-block`으로 묶어 스캔 용이성 개선. 섹션 라벨을 회색→accent로 올리되(대비 5.53→8.01:1) 티커는 흰색으로 내려 패널 안 초록이 한 역할(구조)만 하도록 재배치 — 강조 예산에 범위 한정 예외로 문서화. 크기 7종→5종(10/11/13/15/20px) 수렴은 전역 폰트 정리(7-1)의 파일럿.
- **자간 18종 → 3단계 수렴** — `h3`(섹션 제목) `text-transform:uppercase` 제거 + `1.4px`→`0.3px`(대문자는 `eyebrow`에만 허용하는 금지 목록 3번 위반이었고, 한글엔 uppercase가 무효라 자간만 불필요하게 벌어져 있었음). 배지·지표라벨·알림제목 등 `eyebrow` 계열 9곳(`1 1.2 1.3 1.4 1.5 1.8px`)을 `0.8px`로, 장식성 자간 9곳(`0.3~0.5px`, h2·버튼·그룹제목·공유버튼·태그가이드·푸터)을 `0`으로, 티커류 소형 텍스트 6곳을 `0.3px`로 통일. `.nv-title`(브랜드 픽셀 폰트)은 데스크탑 `1px`/모바일 `0.5px`로 갈라져 있던 걸 `1px`로 통일해 별도 예외로 문서화. `check_design.py`에 자간 검사 추가(허용값 밖 자동 검출).

## 왜 이렇게 정했나 (근거 기록)

- **강조 예산**: 활성 탭 밑줄 91px² vs 이전 카드 좌측 바 10,464px² vs 이전 필 토글 채색 2,618px² — 정보 없는 장식이 진짜 조작 요소보다 최대 100배 강했음 (2026-08-18)
- **회색 9단계**: 실측 60개 중 명도 6~16 구간에 사실상 같은 검정이 8개
- **타이포 7단계**: 33개 중 1px 이내로 인접한(육안 구분 불가) 쌍이 29개
- **`down` 색 교체**: `.negative`가 대비 2.8:1로 AA 미달, 하락 수치가 실질적으로 안 읽히고 있었음
- **전역 `p` 규칙 사고**: `p, .stMarkdown p { color: #606060 !important }`가 Streamlit이 위젯 라벨을 `<p>`로 감싸는 걸 이용해 탭·토글 텍스트까지 덮어씀. 대비 3.18:1로 화면 전체 절반의 텍스트가 AA 미달이었음. 위젯 CSS에서 `p { color: inherit !important }`로 되돌리는 패턴 확립

관련 커밋: `34c5469`(탭 위계), `a6a7052`(구분선 제거), `efcc350`(세그먼트 트랙), `66dac0b`(전역 p 수정), `253c33d`(down 색 수정)
