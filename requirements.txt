# streamlit은 고정 — 내부 DOM(data-testid/data-variant)이 마이너 버전에서 바뀌면
# app.py의 위젯 스코프 CSS가 통째로 무시된다(2026-08-17, 1.57→1.61 탭바 스타일 소실).
# 올릴 땐 로컬에서 같은 버전으로 띄워 탭바·기준 토글·13F 필 3곳을 먼저 확인할 것.
streamlit==1.61.1
yfinance==1.3.0
plotly>=5.22.0
pandas>=2.2.0
google-auth>=2.28.0
